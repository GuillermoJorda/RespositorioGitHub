// Ejercicio 4
// Crear un bucle que contenga todos los números pares 
// desde el 20 hasta el 50 en orden inverso

for (var par=50;par>=20;par--) {
    if ((par%2)==0) {                
        console.log(`${par}`);
    }
}